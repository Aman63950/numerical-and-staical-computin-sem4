#include <stdio.h>

int main() {
    float x=0,y=0,z=0;
    int i;

    for(i=1;i<=10;i++){
        x = (12 - y - z)/10;
        y = (13 - 2*x - z)/10;
        z = (14 - 2*x - 2*y)/10;

        printf("Iteration %d: x=%f y=%f z=%f\n",i,x,y,z);
    }
    return 0;
}
